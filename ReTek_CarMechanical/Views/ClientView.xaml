<Window x:Class="ReTek_CarMechanical.Views.ClientView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReTek_CarMechanical.Views"
        mc:Ignorable="d"
        Title="ClientView" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <Label Content="Keresztnév"/>
            <TextBox Text="{Binding LastName}"></TextBox>
            <Label Content="Vezetéknév"/>
            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Label Content="Születési Hely"/>
            <TextBox  Text="{Binding BirthPlace, UpdateSourceTrigger=PropertyChanged}" ></TextBox>
            <Label Content="Születési Dátum"/>
            <DatePicker SelectedDate="{Binding BirthDate, UpdateSourceTrigger=PropertyChanged}" ></DatePicker>
            <Label Content="TAJ szám"/>
            <TextBox Text="{Binding SocialSecNum, UpdateSourceTrigger=PropertyChanged}" ></TextBox>
            <Label Content="Adószám"/>
            <TextBox Text="{Binding TaxNum, UpdateSourceTrigger=PropertyChanged}" ></TextBox>
            <Button Content="Ügyfél hozzáadása a rendszerhez" Command="{Binding AddNewClientCommand}"></Button>
        </StackPanel>
    </Grid>
</Window>
