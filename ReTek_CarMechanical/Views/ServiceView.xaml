<Window x:Class="ReTek_CarMechanical.Views.ServiceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReTek_CarMechanical.Views"
        mc:Ignorable="d"
        Title="ServiceView" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <Label Content="Válassza ki a kívánt szolgáltatást"></Label>
            <Label Content="Szolgáltatás"></Label>
            <TextBox Text="{Binding ServiceName}"></TextBox>
            <Label Content="Szolgtatás ára"></Label>
            <TextBox Text="{Binding ServicePrice}"></TextBox>
            <Button Content="Szolgáltatás kiválasztása" Command="{Binding AddNewServiceCommand}"></Button>
        </StackPanel>
    </Grid>
            
</Window>
